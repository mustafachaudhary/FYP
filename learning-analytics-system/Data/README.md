# Data Notes

This FYP stores its own local raw copy so it can run independently of the shared source directory.

## Raw Files

- `raw/Events.xlsx`: Moodle event-log source used for sequence extraction.

## Processed Files

The backend generates sessionized outputs and sequence tables under `data/processed/`.